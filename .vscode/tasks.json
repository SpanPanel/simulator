{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Fix markdown formatting",
      "type": "shell",
      "command": "npx",
      "args": [
        "--yes",
        "markdownlint-cli2",
        "--fix",
        "${workspaceFolder}"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "test",
      "presentation": {
        "reveal": "always",
        "panel": "new"
      },
      "problemMatcher": [],
      "detail": "markdownlint-cli2 --fix (uses .markdownlint-cli2.jsonc; requires Node/npx)"
    }
  ]
}
