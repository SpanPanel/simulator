services:
  span-simulator:
    build: .
    network_mode: host
    volumes:
      - ./configs:/app/configs:ro
    environment:
      - CONFIG_DIR=/app/configs
      - TICK_INTERVAL=1.0
      - LOG_LEVEL=INFO
